describe('Main', () => {
  describe('Metodo A', () => {
    context('Case 1', () => {
      it('should happen blablabla', () => {
        // espera que aconteça
        // entrada de dados / metodo sum(2,2)
        // espera retornar (4) => true | (3) => false => broken test
      });
    });
    context('Case 2', () => {

    });
  });

  describe('Metodo B', () => {

  });
});
